spring:
  application:
    name: excel-demo
  datasource:
    url: jdbc:mysql://localhost:3306/test?useUnicode=true&useSSL=false&serverTimezone=Asia/Shanghai&allowMultiQueries=true
    username: root
    password: 123456
    tomcat:
      max-idle: 3
      initial-size: 1
      max-active: 5
      max-wait: 5000

mybatis:
  configuration:
    map-underscore-to-camel-case: true
  mapper-locations: classpath:/mapper/*.xml
server:
  port: 8081